<?php
/**
 * Dynamic QR Secret Configuration Template (P1-G)
 *
 * Copy this file to qr_secret.php and set a strong random value (256-bit+).
 * Generate one with:  php -r "echo bin2hex(random_bytes(32)), PHP_EOL;"
 *
 * IMPORTANT: Do NOT commit qr_secret.php to version control (it is
 * git-ignored). Keep it inside config/ only.
 */

throw new RuntimeException('QR secret not configured. Copy config/qr_secret.php.template to config/qr_secret.php and set a strong random 256-bit secret.');

// return '<64 hex chars from bin2hex(random_bytes(32))>';
